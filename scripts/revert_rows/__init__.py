"""
The revert matrix's rows, split out of `revert_matrix.py` by area.

Each module exports `ROWS`: one stretch of the table, named for the area most of its rows
guard, with every label prefix it holds listed in its docstring. `revert_matrix.py` joins
them into `REVERTS` in the order the rows were written, and `common.py` holds what every row
cites.
"""
